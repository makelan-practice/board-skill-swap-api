services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: skillswap-api:latest
    container_name: skillswap-api
    ports:
      - "8080:80"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:80
    restart: unless-stopped
